# ===========================================
# Fractional Demand Backend - Environment Variables
# ===========================================
# Copy this file to .env and fill in your values
# For Railway deployment, add these as environment variables in your Railway project

# ===========================================
# Database Configuration (Railway PostgreSQL)
# ===========================================
# Railway automatically provides DATABASE_URL when you add a PostgreSQL service
DATABASE_CLIENT=postgres
DATABASE_URL=postgresql://user:password@host:5432/database
DATABASE_SSL=true

# Or use individual variables (DATABASE_URL takes precedence if set)
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=strapi
DATABASE_USERNAME=strapi
DATABASE_PASSWORD=strapi_password_change_me

# ===========================================
# Server Configuration
# ===========================================
HOST=0.0.0.0
PORT=1337
NODE_ENV=production

# PUBLIC_URL is your Railway-provided domain
# e.g., https://your-app-name.up.railway.app
PUBLIC_URL=https://your-app-name.up.railway.app

# ===========================================
# Security Keys (REQUIRED - Generate unique values!)
# ===========================================
# Generate with: openssl rand -base64 32

# JWT secret for user authentication
JWT_SECRET=your-jwt-secret-change-me-in-production

# Admin panel JWT secret
ADMIN_JWT_SECRET=your-admin-jwt-secret-change-me-in-production

# Application keys (comma-separated, at least 4 keys)
APP_KEYS=key1-change-me,key2-change-me,key3-change-me,key4-change-me

# API token salt for generating API tokens
API_TOKEN_SALT=your-api-token-salt-change-me

# Transfer token salt for data transfer features
TRANSFER_TOKEN_SALT=your-transfer-token-salt-change-me

# Encryption key for sensitive data
ENCRYPTION_KEY=your-encryption-key-change-me

# ===========================================
# CORS Configuration
# ===========================================
# Comma-separated list of allowed origins for your frontend(s)
CORS_ORIGINS=http://localhost:3000,http://localhost:3001,https://fractionaldemand.com,https://www.fractionaldemand.com

# ===========================================
# Railway-Specific Notes
# ===========================================
# 1. Add a PostgreSQL service to your Railway project
# 2. Railway auto-injects DATABASE_URL - just set DATABASE_CLIENT=postgres
# 3. Generate all security keys with: openssl rand -base64 32
# 4. Set PUBLIC_URL to your Railway domain after deployment
# 5. Add your production frontend URL to CORS_ORIGINS
